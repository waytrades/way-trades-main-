import defu from "defu";
import { useRuntimeConfig } from "#imports";
export const useSanityConfig = () => {
  const $config = useRuntimeConfig();
  return defu(
    import.meta.server ? $config.sanity : {},
    $config.public.sanity
  );
};
